# Motion and colour update — September 9, 2026

The meal-finder heading now sits above the quiz instead of in a narrow sticky column. Solid cream, forest green, terracotta and blue surfaces replace gradient washes. The shared identity is a divided plate, with coordinated homepage cards and legible theme changes.

Quiz transitions fade the outgoing question, reposition once, and reveal the next heading. Removed smooth-scroll/height-release races and the extra post-scroll button lock. Above-fold content no longer replays scroll entrances. Page navigation has no artificial delay or image/ad-dependent arrival fade. Reduced-motion and pause controls remain supported.

The homepage illustration now reserves its dimensions before loading. In delayed-resource mobile tests, its measured layout shift fell from 0.0987 to 0.0000. The calculator and search also measured 0.0000; the quiz measured 0.0124. These are local controlled tests, not field performance claims.

Validation: all 73 pages checked at 320/768/1440 pixels in both themes for clipping, overflow and broken images; contrast audit clean in both themes. Active-motion quiz navigation passed Edge and WebKit at 320/390/1440, including results and restart. Meal browsing, calculator controls, mobile menus, reduced motion and the full isolated build passed. The AdSense publisher script and reviewed nutrition data remain intact.
